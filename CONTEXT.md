# Git Diff Editor 프로젝트 컨텍스트

## 문서 역할

프로젝트의 용어와 지속되는 결정은 이 파일을 기준으로 한다. 상세 요구사항은 REQUIREMENTS.md, 구현 아키텍처는 ARCHITECTURE.md, 기술 설계 제안과 구현 순서는 IMPLEMENTATION_PLAN.md를 참조한다. 충돌 시 최근 사용자 명시 결정을 반영하고 관련 문서를 함께 정정한다.

## 목적

바이브 코딩으로 변경된 로컬 Git 코드를 좌측 원본/우측 변경 화면으로 비교하고 우측에서 편집·명시적 저장하는 Windows 독립 실행형 도구. 종합 IDE나 Git 관리 프로그램이 아니다.

## 확정 결정

- HEAD 대 현재 작업 트리 비교. 인덱스 전용 비교는 하지 않는다.
- Tauri + React + TypeScript + Monaco Editor 구조를 사용한다.
- Git, 파일 시스템, 파일 감시는 Rust 백엔드가 담당하고 시스템 Git CLI를 사용한다.
- diff의 시각적 계산은 프런트엔드, 디스크·Git 상태의 기준은 Rust가 담당한다.
- 타입 기반 Tauri Command/Event IPC와 Zustand 상태 저장소를 사용한다.
- 좌측 원본 읽기 전용, 우측 편집 가능. 변경되지 않은 텍스트 파일도 우측 편집 가능.
- 문법 강조 없음. 줄 번호와 추가·삭제·수정 diff 색상을 제공한다.
- 좌우 파일에는 diff 정렬용 가상 공백 행을 삽입하지 않는다. 각 파일의 실제 줄 구조와 줄 번호를 그대로 표시한다.
- 대응되는 변경 묶음은 중앙 연결선과 선택 강조로 연결하고, F7/F8 이동 시 양쪽 패널을 각각 대응 위치로 이동한다.
- 변경 묶음과 전체 변경량을 `-삭제 줄 / +추가 줄` 형태로 표시한다.
- 자동 줄바꿈 없음, 가로 스크롤. 공백·탭 기호는 기본 숨김, 변경 공백은 diff 표시.
- 전체 파일을 폴더 계층으로 표시하고 변경 자손이 있는 폴더를 우선한다. 폴더 접기·펼치기를 제공하며 파일 검색/필터는 제외한다.
- 사이드바와 좌우 비교 패널 너비는 드래그 또는 키보드로 조절하며 사용자 설정을 보존한다.
- 저장소·파일 로딩과 저장 진행 상태를 구분해 표시하고, 실패한 사용자 작업에는 재시도 동작을 제공한다.
- 미추적 파일은 U, Git이 판정한 이름 변경만 R, 그 외 이동은 D+A.
- detached HEAD와 worktree 지원. bare 저장소·submodule·병합 충돌·외부 링크 대상 편집 제한.
- 저장 버튼/Ctrl+S로만 디스크 저장. Rust 임시 파일 작성 후 교체 저장.
- 외부 변경이 우선이며 확인창 없이 자동 새로고침한다. 같은 파일의 미저장 편집도 외부 내용으로 교체한다.
- 다른 파일의 외부 변경으로 현재 파일 편집을 폐기하지 않는다.
- 사용자 요청에 의한 파일/저장소 전환·정상 종료에는 미저장 저장/버리기/취소 적용. 외부 재로드는 이 확인 절차의 예외이다.
- Git 스테이징/커밋/브랜치 전환/복원 제외.
- Windows 포터블 배포, PC에 설치된 Git 사용.
- UTF-8 및 기존 줄바꿈 형식 보존. 혼합 줄바꿈 파일은 읽기 전용.
- Tauri 최소 권한만 허용하고 네트워크 권한은 사용하지 않는다.
- 최근 저장소·레이아웃 설정은 Windows 사용자 앱 데이터 영역에 저장한다.
- 고정 파일 크기 제한은 두지 않고 성능 위험 파일은 읽기 전용 또는 미지원으로 처리한다.

## 용어

- 원본: HEAD 커밋의 파일 내용.
- 작업 트리: 디스크의 현재 파일 내용.
- 편집 버퍼: 아직 저장하지 않았을 수 있는 우측 텍스트.
- dirty: 읽기/저장 기준과 편집 버퍼가 다른 상태. Git의 M 상태와 별개.
- 변경 묶음(hunk): 서로 인접한 삭제·추가·수정 범위를 하나로 묶은 비교 단위.
- 자동 새로고침: 외부 디스크 변경을 읽어 화면에 반영. 자동 저장을 뜻하지 않는다.

## 현재 산출물 및 작업 경계

- REQUIREMENTS.md: 요구사항과 수용 테스트 정의.
- IMPLEMENTATION_PLAN.md: 모듈 경계, 상태 전이, 외부 갱신/저장 설계, 단계별 검증 게이트.
- ARCHITECTURE.md: 프로세스 경계, 데이터 흐름, 상태 머신, IPC와 보안 경계.
- FILETREE.md: Tauri/Rust/React 소스 구조와 모듈 매핑.
- IMPLEMENTATION_DETAIL.md: Command/Event, Git 파싱, 저장·감시, Monaco/Zustand 구현 단위.
- 제품 소스: `src/` (React), `src-tauri/` (Rust).
- 테스트는 임시 저장소와 브라우저 E2E 브리지를 사용하며 사용자 실제 저장소를 임의로 수정하지 않는다.

## 변경 이력

- 초기 인터뷰는 본 대화에서 진행했으며 별도 일자는 기록하지 않음.
- 최초 작성: 구현 계획 설계 요청에 따라 기존 프로젝트 폴더에 컨텍스트 추가. 외부 변경 우선 정책을 고정 결정으로 기록.
- 2026-09-21: 원본 보존형 비교 UX를 명시하고 P1 변경 묶음 연결, 요약, 리사이저, 폴더 트리, 로딩·재시도 동작을 반영.
