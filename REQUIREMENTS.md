# Git Diff Editor 요구사항 정의서

- 문서 버전: 1.3
- 상태: 인터뷰 확정사항 및 아키텍처 그릴링 결정 반영 — 구현 착수 승인 아님
- 프로젝트 폴더: `C:\Users\gbsong\git-diff-editor`
- 변경 이력: 1.3 — Tauri/Rust/React/TypeScript/Monaco/Zustand 구조, 외부 변경 우선, 파일 트리·특수 Git 상태·텍스트 처리 결정을 반영.

## 1. 목적과 범위

바이브 코딩 도구가 수정한 로컬 Git 저장소를 독립 실행형 프로그램에서 확인한다. 왼쪽에는 원본 코드, 오른쪽에는 현재 변경 코드를 나란히 표시하고, 오른쪽에서 직접 편집한 뒤 명시적으로 저장한다.

핵심은 **변경 내용의 명확한 비교와 우측 편집**이다. 종합 IDE나 Git 관리 도구로 확장하지 않는다.

- 사용자: Windows에서 바이브 코딩을 진행하는 개발자
- 배포: 설치 없이 실행하는 Windows 포터블 형태
- Git: 사용자 PC에 설치된 Git 사용
- 데이터: 로컬 저장소의 파일과 Git 정보
- 구현 기술과 최소 Windows 버전은 아직 미정

### 1.1 요구사항의 승인 상태

- **확정**: 인터뷰에서 사용자가 직접 요구하거나 추천에 동의한 사항
- **명세화**: 확정된 기능의 의미를 일관되게 설명한 사항
- **제안/미결정**: 제품 동작 또는 지원 범위를 추가로 결정해야 하는 사항. 사용자 승인으로 간주하지 않는다.
- 이 문서의 테스트 항목은 구현 검증 기준이지 실제 테스트 통과 기록이 아니다.
- 8절의 제안은 확정된 요구사항을 덮어쓰지 않는다.

## 2. 용어와 비교 모델

- `HEAD`: 현재 체크아웃된 커밋. 좌측 원본의 기준이다.
- 작업 트리: 디스크에 존재하는 현재 파일 내용이다.
- 인덱스: Git 스테이징 영역. 별도의 비교 패널이나 편집 대상으로 제공하지 않는다.
- 편집 버퍼: 우측 화면에서 편집 중인 메모리상의 내용이다.
- 미저장 상태(dirty): 편집 버퍼가 마지막 읽기/저장 기준과 다른 상태이다.
- Git 변경 상태: `HEAD`와 현재 디스크 상태의 차이이다. 미저장 상태와 구별한다.
- 비교 스냅샷: 화면을 구성할 때 읽은 HEAD 커밋 ID, 파일 경로, 디스크 내용의 조합이다.

### 2.1 HEAD 대 작업 트리의 의미 — 명세화

- 스테이징된 파일도 최종 디스크 내용이 HEAD와 다르면 비교 대상이다.
- 스테이징 이후 다시 수정된 파일도 우측에는 인덱스가 아닌 최신 작업 트리 내용을 표시한다.
- 인덱스만 다르고 디스크 내용이 HEAD와 같다면 본 화면의 코드 diff는 없다. 스테이징 상태 전용 화면이 아님을 명시한다.
- 우측 편집 중에는 HEAD와 편집 버퍼를 비교한다. 저장하기 전에는 디스크나 인덱스에 쓰지 않는다.
- 저장은 작업 트리 파일만 변경한다. HEAD, 인덱스, 브랜치는 변경하지 않는다.
- 좌측 패널에는 읽은 HEAD의 짧은 커밋 ID를 함께 표시하는 방안을 제안한다. 외부 커밋/체크아웃 후 오래된 원본과 혼동하지 않도록 하기 위함이다.

## 3. 확정된 기능 요구사항

### FR-001. 저장소 열기

- 폴더 선택 창으로 Git 저장소를 열 수 있어야 한다.
- 최근 열었던 저장소 목록을 제공한다.
- 현재 저장소 경로를 표시한다.
- 잘못된 저장소 또는 Git 오류를 안내하고 다른 저장소를 선택할 수 있어야 한다.
- Git 설치 여부 및 버전을 확인한다.
- `.git` 폴더 유무만으로 저장소를 판정하지 않는다. `.git`이 파일인 Git worktree도 있으므로 Git 명령의 저장소 판정 결과를 사용한다. 지원 범위는 O-04에서 정한다.

### FR-002. 비교 기준과 Git 권한

- 현재 HEAD와 작업 트리를 비교한다.
- 현재 브랜치 이름을 표시한다.
- 브랜치 전환, 스테이징, 커밋, 복원 기능은 제공하지 않는다.
- 조회·편집·파일 저장만 수행한다.

### FR-003. 파일 사이드바

- 변경 파일만이 아니라 저장소 전체 파일을 디렉터리 구조로 표시한다.
- 변경 파일을 상태 문자와 색상/아이콘으로 구분한다: `M` 수정, `A` 추가, `D` 삭제, `R` 이름 변경.
- 변경 파일을 우선 정렬한다. 디렉터리 구조와 우선 정렬의 조합은 O-01에서 정한다.
- `.gitignore` 대상은 기본적으로 제외한다. 단, 이미 Git이 추적하는 파일은 ignore 패턴에 일치해도 표시한다.
- `.git` 내부 메타데이터는 사용자 코드 트리에 노출하지 않는다.
- 디스크에서 삭제된 파일도 HEAD 경로를 이용하여 삭제 상태로 표시한다.
- 파일명 검색과 상태 필터는 제외한다. 코드 내부 검색과 혼동하지 않는다.
- 폴더 접기·펼치기는 Q7의 트리 동의 이후 Q24에서 거절한 묶음에도 포함되어 있었으므로 확정으로 단정하지 않는다. O-01에서 확인한다.

### FR-004. 좌우 코드 비교

- 좌측: HEAD 원본, 읽기 전용
- 우측: 작업 트리 또는 편집 버퍼, 편집 가능
- 문법 강조 없이 일반 텍스트로 표시한다.
- 줄 번호, 추가·삭제·수정 줄 색상, 좌우 줄 정렬, 동기 스크롤을 제공한다.
- 문법 강조 제외는 diff 색상 제외를 뜻하지 않는다.
- 추가/삭제로 줄 수가 다르면 화면상의 정렬용 빈 공간으로 대응 위치를 맞춘다. 그 공간은 실제 텍스트나 저장 데이터가 아니다.
- 양쪽 줄 번호는 각 파일의 실제 줄 번호이며 정렬용 빈 공간에는 번호를 붙이지 않는다.
- 동일한 코드 위치를 기준으로 수직 스크롤을 동기화한다. 단순 픽셀 비율 동기화로 위치가 어긋나지 않아야 한다.
- 변경되지 않은 영역 접기와 미니맵은 제외한다.

### FR-005. 파일 유형

- 수정: 양쪽 내용을 비교한다.
- 추가: 좌측 빈 화면, 우측 전체 내용 및 편집 가능.
- 삭제: 좌측 원본, 우측 빈 화면 및 편집 불가. 저장으로 삭제 파일을 복원하지 않는다.
- 이름 변경: 이전 경로와 현재 경로를 연결하여 표시하고 내용 변경도 비교한다.
- 바이너리: 상태는 표시하되 텍스트 비교/편집은 하지 않고 미지원 사유를 안내한다.
- 지원하지 않는 인코딩: 깨진 문자를 편집 가능한 텍스트로 제공하지 않는다.
- 미추적 파일 및 이름 변경 판정의 세부 정책은 O-02/O-03에서 정한다.

### FR-006. 편집과 실시간 diff

- 우측 내용을 변경하면 저장 전에도 diff를 갱신한다.
- 미저장 여부를 화면에 명확하게 표시한다.
- diff 재계산으로 커서, 선택 영역, 입력 조합, 스크롤 위치를 불필요하게 초기화하지 않는다.
- 이전 계산이 늦게 완료되어 최신 편집 결과를 덮어쓰지 않아야 한다.
- `Ctrl+Z` 실행 취소, `Ctrl+Y` 다시 실행을 제공한다.
- 실행 취소로 읽기/저장 기준 내용과 같아지면 미저장 표시를 해제한다.
- 한글 입력 조합 중 diff 갱신이 입력 내용을 훼손하지 않아야 한다.

### FR-007. 명시적 저장과 이탈 보호

- 저장 버튼 또는 `Ctrl+S`로 현재 우측 버퍼를 실제 파일에 저장한다.
- 자동 저장은 하지 않는다.
- 저장 성공 시 미저장 표시를 해제하고 디스크 기준 Git 변경 상태를 갱신한다.
- 파일 저장 성공과 Git 상태 재조회 성공을 구분한다. 재조회만 실패한 경우 파일 저장 자체가 실패했다고 표시하지 않는다.
- 미저장 상태에서 사용자가 파일 전환, 저장소 전환, 정상 종료를 요청하면 저장·버리기·취소를 제공한다. 외부 변경에 의한 자동 새로고침은 예외이며 FR-008을 적용한다.
- 저장 선택: 저장 성공 후에만 전환/종료한다. 실패하면 원래 화면과 버퍼를 유지한다.
- 버리기 선택: 메모리 편집만 폐기하며 디스크에 예전 내용을 다시 쓰지 않는다.
- 취소 선택: 전환/종료를 취소하고 현재 편집을 유지한다.
- 강제 프로세스 종료나 전원 장애에서 미저장 버퍼 복구까지 보장하는 기능은 합의되지 않았다.

### FR-008. 외부 변경 우선·자동 새로고침 — 확정

- 다른 편집기/바이브 코딩 도구가 변경한 디스크 내용을 우선한다.
- 외부 변경 감지 시 사용자 확인 없이 해당 파일과 diff, 트리 상태를 자동 새로고침한다.
- 같은 파일에 미저장 편집이 있어도 외부 내용을 우선하여 버퍼를 교체하고 미저장 표시를 해제한다. 해당 미저장 편집은 폐기된다.
- 덮어쓰기/취소 확인창이나 수동 새로고침 선택을 요구하지 않는다.
- 다른 파일의 외부 변경만으로 현재 편집 중인 파일의 버퍼를 폐기하지 않는다.
- 외부 삭제는 삭제 상태로 갱신하며 오래된 버퍼를 저장해 파일을 재생성하지 않는다. 이동은 Git 판정에 따라 트리를 갱신한다.
- 외부 커밋/브랜치 전환 시 HEAD 및 작업 트리 비교를 자동 갱신한다. 파일 내용이 바뀐 경우 외부 변경 우선 규칙을 적용한다.
- 저장 직전 미반영 외부 변경을 발견해도 오래된 버퍼로 덮어쓰지 않고 최신 디스크 내용으로 자동 갱신한다.
- 재읽기에 실패하면 오류와 재시도 상태를 표시하며 성공한 새로고침으로 취급하지 않는다. 오래된 내용을 디스크에 자동 기록하지 않는다.
- 프로그램 자체의 저장 이벤트는 외부 변경과 구별하여 중복 갱신이나 새 편집의 오폐기를 방지한다.
- 자동 새로고침은 디스크 읽기이며 자동 저장과 다르다. 우측 편집의 명시적 저장 원칙은 유지한다.

### FR-009. 화면과 단축키

- 상단: 저장소 경로, 현재 브랜치명, 저장 버튼
- 사이드바: 전체 파일 트리와 변경 상태
- 본문: 원본/변경 좌우 패널
- 사이드바 및 좌우 코드 패널의 구분선을 드래그하여 크기를 조절한다.
- `Ctrl+S`: 현재 편집 파일 저장
- `Ctrl+Z` / `Ctrl+Y`: 우측 편집 실행 취소/다시 실행
- `Ctrl+F`: 현재 코드 화면 내부 검색. 파일 검색은 제공하지 않는다.
- 좌측에서는 단축키나 붙여넣기로 원본을 수정할 수 없어야 한다.
- 키보드만으로 파일 트리를 탐색하는 것은 접근성 제안이며 독립된 추가 필수 기능으로 확정하지 않는다.

### FR-010. 상태 표시

- 변경사항이 없으면 전체 파일 트리를 유지하고 초기 코드 영역에 “변경사항이 없습니다”를 표시한다.
- 변경 없는 파일 선택 시 비교/편집 허용 여부는 O-06에서 정한다.
- 로딩, 변경 없음, 파일 선택 안 됨, 미지원, 오류를 서로 구별한다.
- 읽기/Git 명령 실패를 “변경사항 없음”으로 표시하지 않는다.
- 오류에는 원인과 가능한 조치를 표시하며 프로그램 전체를 종료하지 않는다.

## 4. 비기능 요구사항과 안전 조건

### NFR-001. 플랫폼·배포

- Windows 포터블 프로그램, 시스템에 설치된 Git 사용.
- 관리자 권한과 Git 재로그인을 요구하는 제품 기능은 없다.
- 최소 OS/Git 버전, 실행 파일 외 런타임 필요 여부, 포터블 패키지 구성은 구현 결정 항목이다.

### NFR-002. 파일 내용 보존

- UTF-8을 기본 지원하며 기존 LF/CRLF 형식을 유지한다.
- 저장 과정에서 자동 포맷팅, 공백 제거, 전체 파일 줄바꿈 변경을 수행하지 않는다.
- 내용 보존 명세로 UTF-8 BOM 유무 및 파일 끝 개행 유무를 유지한다.
- 혼합 줄바꿈 파일의 편집 지원 여부는 O-07에서 결정한다. 임의 정규화는 금지한다.
- 저장할 수 없는 파일은 오류를 알리고 편집 버퍼를 유지한다.

### NFR-003. 성능과 응답성

- 일반적인 코드 저장소를 우선 지원하며 대규모 저장소 특화 최적화는 제외한다.
- 파일 목록 지연 로딩은 합의된 목표를 위한 구현 후보이지 특정 아키텍처 강제가 아니다.
- 파일 읽기/Git 실행/diff 계산 중에도 화면이 장시간 응답 불능이 되지 않도록 한다.
- 새 파일/저장소 선택 이후 완료된 이전 요청 결과를 현재 화면에 표시하지 않는다.
- 지원 파일 크기·줄 수, 기준 PC, 저장소 규모, 초기 로드 및 diff 응답 시간의 수치는 아직 미정이다. O-08 승인 및 실측 전에는 성능 보장을 주장하지 않는다.

### NFR-004. 저장소 안전성 — 기술 검토 기준

- 파일 경로는 명령 문자열에 이어 붙이지 않고 인자로 전달한다. 공백·한글·특수문자 경로를 보존한다.
- 저장소 검사 과정에서 전역 Git 설정이나 safe.directory를 자동 변경하지 않는다.
- 조회를 위해 외부 diff/textconv 프로그램이나 저장소 스크립트를 자동 실행하지 않는 방식을 사용한다.
- 코드 비교를 위해 소스 코드를 외부 서버에 보내는 기능은 이 범위에 없다.
- 심볼릭 링크·junction·submodule의 외부 경로에 무심코 쓰지 않도록 지원 여부를 명시한다(O-04).
- 파일 저장 중 기존 파일을 먼저 비우는 방식은 피한다. 교체 저장 등 구체적 보호 방법과 실패/경합 한계는 구현 설계에서 검증한다.
- 저장 완료로 표시하기 전에 쓰기 결과를 확인한다. 외부 프로세스와의 완전한 동시성 보장은 구현 검증 없이 약속하지 않는다.

## 5. 주요 오류 및 기대 동작

- Git 미설치/실행 불가: 원인 및 설치/경로 확인 안내, 재시도 가능.
- 비저장소 폴더: 다른 폴더 선택 가능. `.git` 폴더가 반드시 있어야 한다는 잘못된 안내는 하지 않는다.
- 저장소 접근 거부/소유권 제한: 정책을 임의 변경하지 않고 사유 안내.
- 파일 읽기 실패: 해당 파일 오류로 표시하고 다른 파일 탐색 유지.
- 인코딩/바이너리 미지원: 내용을 깨진 텍스트로 편집시키지 않고 상태만 표시.
- 저장 실패: 미저장 버퍼 유지, 저장 성공 표시 금지, 재시도 가능.
- 외부 변경 충돌: 외부 디스크 내용 우선으로 자동 새로고침한다. 해당 파일의 미저장 편집은 폐기하며 확인창을 요구하지 않는다(FR-008/O-05).
- Git 조회 시간 초과/오류: 오류와 재시도 제공. 변경 없음으로 취급하지 않음.
- 최근 저장소 경로 소실: 목록 오류 안내 및 다른 저장소 선택. 저장소를 임의 재생성하지 않음.

## 6. 명시적 제외 범위

- 문법 강조, 자동 포맷팅
- 파일명 검색, 변경 상태 필터
- 스테이징, 커밋, 브랜치 전환, Git 복원
- 좌측 편집, 자동 저장
- 미니맵, 변경 없는 영역 접기
- 대용량 파일 특화 최적화
- macOS/Linux, 설치 프로그램, Git 번들링
- 자동 병합, Git 충돌 해결 UI, 파일 생성/삭제/이름 변경을 수행하는 파일 관리자 기능

마지막 줄은 합의된 비교·편집·저장 범위 밖의 기능임을 명시한 것이다. 이를 후속 개발 약속으로 해석하지 않는다. 범위 밖 기능은 별도 승인 없이 P2 백로그로 자동 편입하지 않는다.

## 7. 구현 전 기술 결정

다음은 사용자 기능 요구와 분리된 구현 결정이다. 구체적인 도구를 미리 확정하지 않는다.

- 데스크톱 프레임워크와 편집 컴포넌트
- 줄 정렬 및 diff 계산 방식
- Git 프로세스 실행, NUL 구분 경로 파싱, 종료/타임아웃 처리
- 파일 변경 감지와 저장 직전 검증 방식
- 안전한 파일 저장 및 권한 보존 방식
- UTF-8/BOM/줄바꿈 보존 방식
- 최근 저장소 목록과 UI 설정의 저장 위치
- Windows 패키징, 최소 OS/Git 버전, 필요한 런타임
- 테스트 자동화 및 실제 Windows GUI 검증 방식

## 8. 상세 정책 결정 기록 — 미결정 항목만 한 번에 하나씩 확인

### O-01. 트리와 변경 우선 정렬 — 확정

- 디렉터리 구조를 유지한다.
- 각 폴더 내부에서 변경 파일과 변경 자손이 있는 폴더를 우선 정렬한다.
- 폴더 접기·펼치기를 제공한다.
- 파일 검색/필터는 제공하지 않는다.
- 상태: 확정.

### O-02. 미추적 파일 — 확정

- 무시되지 않은 미추적 파일은 `U` 상태로 표시한다.
- 미추적 파일도 텍스트라면 우측 편집을 허용한다.
- `.gitignore` 대상 미추적 파일은 표시하지 않는다.
- 상태: 확정.

### O-03. 이름 변경 판정 — 확정

- Git이 이름 변경으로 판정한 경우에만 `R`로 표시한다.
- 판정하지 못한 이동은 `D`와 `A`로 표시한다.
- 상태: 확정.

### O-04. 특수 Git 상태/파일 — 확정

- detached HEAD와 Git worktree는 지원한다.
- bare 저장소·submodule·병합 충돌 상태는 안내하고 관련 편집을 제한한다.
- 심볼릭 링크·junction이 저장소 외부를 가리키면 읽기·쓰기를 차단한다.
- 저장소를 자동 변경하여 해결하지 않는다.
- 상태: 확정.

### O-05. 외부 변경 우선 — 확정

- 사용자 명시: “외부변경이 우선이고 자동새로고침”.
- 외부 디스크 내용이 현재 파일의 미저장 버퍼보다 우선한다. 자동 새로고침하며 사용자 확인을 요구하지 않는다.
- 저장 차단 후 버퍼 유지, 덮어쓰기 확인창, 수동 새로고침을 선택하게 하는 기존 제안은 채택하지 않는다.
- 상태: 확정. 상세 동작은 FR-008, 검증은 AC-014/AC-015 참조.

### O-06. 변경 없는 파일 — 확정

- 저장소 초기 화면에는 “변경사항이 없습니다”를 표시한다.
- 파일을 선택하면 동일한 내용을 좌우로 표시하고 우측 편집을 허용한다.
- 상태: 확정.

### O-07. 텍스트 예외 — 확정

- 혼합 줄바꿈 파일은 읽기 전용으로 표시한다.
- Ctrl+F는 활성 코드 패널의 단순 문자열 검색으로 제공한다. 정규식·치환은 제외한다.
- 상태: 확정.

### O-08. 지원 한계와 배포 검증 — 부분 확정

- 고정 파일 크기·저장소 규모 제한은 두지 않는다.
- 성능 위험 파일은 읽기 전용 또는 미지원 상태로 안내한다.
- 최소 Windows/Git 버전, 실측 기준, 응답 시간은 구현 단계에서 결정한다.
- 상태: 부분 확정.

## 9. 수용 테스트 목록

각 테스트는 사전 데이터, 동작, 확인 결과를 기록한다. O 항목과 연결된 테스트는 해당 정책 승인 후 기대 결과를 확정한다.

- **AC-001 / FR-001**: 유효한 저장소 열기 → 경로, 브랜치, 파일 트리 표시. 앱 재실행 후 최근 저장소로 다시 열기.
- **AC-002 / FR-002, FR-004**: 수정 파일 선택 → 좌측 HEAD, 우측 디스크 내용 일치 및 변경 줄 표시.
- **AC-003 / FR-002**: 스테이징된 수정, 이후 재수정, 인덱스만 다른 사례 → 우측은 항상 최종 작업 트리 기준이며 인덱스는 변경되지 않음.
- **AC-004 / FR-003**: 추적 파일, 무시된 미추적 파일, ignore 패턴에 일치하는 추적 파일 → 추적 파일 유지, 무시된 미추적 파일 제외, .git 제외.
- **AC-005 / FR-005**: 추가/삭제 파일 선택 → 한쪽 빈 화면, 추가만 우측 편집 가능, 삭제 복구 없음.
- **AC-006 / FR-005, O-03**: 이름만 변경/이름과 내용 변경/판정 불가 → 승인된 R 또는 D+A 정책에 따라 누락 없이 표시.
- **AC-007 / FR-004**: 중간 줄 추가·삭제가 섞인 파일 → 대응 줄 정렬, 각 실제 줄 번호 정확, 동기 스크롤 위치 일치. 정렬용 공백이 저장되지 않음.
- **AC-008 / FR-006**: 우측 편집 및 한글 입력 → diff 갱신, 입력/커서 유지, 디스크는 저장 전 불변.
- **AC-009 / FR-006**: 수정 후 실행 취소/다시 실행 → 내용과 dirty 표시 일치.
- **AC-010 / FR-007**: 버튼/Ctrl+S 저장 → 디스크 내용 일치, dirty 해제, HEAD/인덱스 불변.
- **AC-011 / FR-007**: 미저장 파일에서 파일·저장소 전환/정상 종료 → 저장·버리기·취소 각 분기 검증. 버리기는 디스크에 쓰지 않음.
- **AC-012 / FR-007**: 권한 제한 등으로 저장 실패 → 버퍼 유지, 오류 표시, 전환/종료 진행 금지.
- **AC-013 / FR-007**: 저장 성공 후 Git 재조회 실패 → 저장 성공과 재조회 오류를 구별.
- **AC-014 / FR-008, O-05**: 미저장 편집 중 같은 파일을 외부 수정 → 확인창 없이 최신 외부 내용으로 버퍼/diff 갱신, dirty 해제, 이전 편집 폐기. 외부 삭제 시 삭제 상태 반영 및 오래된 버퍼로 재생성하지 않음. 다른 파일 변경 시 현재 편집 유지. 저장 직전 발견한 외부 변경에도 같은 우선순위 적용. 자체 저장 이벤트가 이후 새 편집을 폐기하지 않는지 검증.
- **AC-015 / FR-008**: 외부 커밋/체크아웃 → 사용자 확인 없이 HEAD/작업 트리 비교 자동 갱신. 내용이 변경된 파일은 외부 우선 규칙 적용. 재읽기 실패 시 오류 표시 및 오래된 버퍼의 자동 디스크 쓰기 없음.
- **AC-016 / FR-009**: 패널 크기 조절, 코드 검색, 좌측 붙여넣기 시도 → 레이아웃 조절/검색 동작, 좌측은 불변.
- **AC-017 / FR-010, O-06**: 깨끗한 저장소 열기 → 트리와 변경 없음 안내. 파일 선택은 승인된 정책 적용.
- **AC-018 / NFR-002**: UTF-8 한글, BOM 유/무, LF/CRLF, 끝 개행 유/무 → 의도한 편집 외 바이트 변화 없음.
- **AC-019 / FR-005, O-07**: 바이너리/미지원 인코딩/혼합 줄바꿈 → 승인된 제한 안내, 손상 저장 없음.
- **AC-020 / NFR-004**: 공백·한글·특수문자 경로 → 정확한 파일 조회/저장, 명령 주입 없음.
- **AC-021 / FR-001, FR-010**: Git 미설치/비저장소/접근 거부/조회 실패 → 원인 표시, 다른 저장소 선택 또는 재시도, 변경 없음으로 오표시하지 않음.
- **AC-022 / NFR-003**: 파일 빠르게 연속 선택 및 연속 편집 → 이전 비동기 결과가 최신 화면을 덮어쓰지 않음.
- **AC-023 / O-02, O-04**: 미추적/초기 커밋 없음/detached HEAD/bare/worktree/충돌/submodule/링크 → 각각 승인된 지원/제한 정책 검증.
- **AC-024 / NFR-001, O-08**: 승인된 Windows 환경에서 포터블 패키지 실행 → 사전 설치 조건 안내 및 핵심 열기·비교·편집·저장 가능.
- **AC-025 / NFR-003, O-08**: 승인된 규모와 제한 초과 파일 → 응답 시간 실측 기록, 초과 파일 처리 중 다른 UI 사용 가능.

## 10. 구현 순서와 완료 판정

### 10.1 권장 구현 순서 — 범위 축소 아님

1. 저장소 판정, 비교 모델 및 파일 상태 테스트
2. 전체 파일 트리, 좌우 diff와 줄 정렬
3. 우측 편집, 실행 취소/다시 실행, 실시간 diff
4. 명시적 저장, 이탈 보호, 외부 변경/충돌 안전성
5. 단축키, 오류 UI, 인코딩 보존, Windows 패키징 검증

순서는 구현 편의를 위한 제안이다. 기존 확정 기능을 낮은 우선순위라는 이유로 완료 기준에서 제외하지 않는다.

### 10.2 착수 전 확인

- O-01~O-08 중 미결정 항목만 필요한 순서대로 하나씩 확인한다. O-05는 확정되었으므로 재질문하지 않는다.
- UI 기술, 편집 컴포넌트 및 배포 방식은 기술 검토 결과로 결정한다.
- 요구사항 정리와 구현 착수 승인을 구분한다.

### 10.3 1차 완료 기준

- 확정 기능 FR-001~FR-010과 승인된 상세 정책이 구현되어야 한다.
- 관련 AC 테스트별 실행 환경, 입력 fixture, 실제 결과, 실패/제한 사항을 기록해야 한다.
- 파일 저장/외부 변경/인코딩 보존 테스트는 실제 임시 Git 저장소와 실제 파일 I/O로 수행한다.
- GUI의 줄 정렬·스크롤·입력·단축키·크기 조절은 실제 Windows 앱에서 확인한다.
- 포터블 산출물을 실행해 저장소 열기부터 편집·저장까지 검증한다.
- 문서 작성, mock 테스트, 빌드 성공만으로 제품 완료를 선언하지 않는다.
